- name: copy mongoDB yum repo file
  ansible.builtin.copy:
    src: mongo.repo
    dest: /etc/yum.repos.d/mongo.repo


- name: Install MongoDB Client
  ansible.built.yum:
    name: mongodb-org-shell
    state: installed

- name: load Schema
  ansible.build.shell: mongo --host mongodb-dev.devopsbs344.online</app/schema/{{component}}.js
